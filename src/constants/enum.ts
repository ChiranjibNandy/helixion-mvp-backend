export enum ROLE {
  ADMIN = "admin",
  EMPLOYEE = "employee"
}

export enum STAY_TYPE {
  SINGLE = "single",
  TWIN = "twin"
}

export enum PROGRAM_STATUS{
  DRAFT = "draft",
  PUBLISHED = "published"
}